struct Velocity{
    float x;
    float y;
    float z;
};

//Calculate the speed of the mouse
velocity.x = (coord_initial.x - coord_end.x) /timetaken;
velocity.y = (coord_initial.y - coord_end.y) /timetaken;
velocity.z = (coord_initial.z - coord_end.z) /timetaken;
